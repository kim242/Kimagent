# Kimagent — dépendances Python
# Installation : python3 -m venv .venv && .venv/bin/pip install -r requirements.txt

mcp>=2.0
anthropic>=0.40
openai>=1.50
python-dotenv>=1.0
httpx>=0.27

# Interface graphique web (python -m kimagent gui)
flask>=3.0
markdown>=3.5
